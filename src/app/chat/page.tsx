import ChatContainer from "@/components/chat/ChatContainer";

export const metadata = {
  title: "Sabha — Kurukshetra",
  description: "Ask the oracle of the Mahabharata.",
};

export default function ChatPage() {
  return <ChatContainer />;
}
